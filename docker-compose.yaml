version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - PORT=${PORT}
        - DEBUG=${DEBUG}
        - GCR=${GCR}
    ports:
      - "${PORT}:${PORT}"
    environment:
      - PORT=${PORT}
      - DEBUG=${DEBUG}
      - GCR=${GCR}
    env_file:
      - .env